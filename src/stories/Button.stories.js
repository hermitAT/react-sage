import React from "react";

import Button from "components/Button";

export default {title: "button"};
export const primary = () => <Button confirm>confirm</Button>
export const secondary = () => <Button cancel>cancel</Button>
